pipeline {
    agent any
        stages {
            stage('build') {
                sh 'javac main1.java'
            }
            stage('run') {
                sh 'java Main'
            }
        }
}
